# SupportBot AI Web Widget

This directory will contain the lightweight, client-side embeddable JavaScript widget (`embed.js`).

> **Note**: Widget client code will be implemented in subsequent phases.
