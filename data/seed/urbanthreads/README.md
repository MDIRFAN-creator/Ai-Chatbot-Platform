# UrbanThreads Seed Data

This directory will contain initial development seed information used to populate the V1 database during development.

> **Note**: Seed data will be implemented in subsequent phases. The production application treats the structured SQLite database as its authoritative source of truth.
